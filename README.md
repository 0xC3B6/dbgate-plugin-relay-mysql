# DbGate Relay MySQL Plugin

Read-only MySQL access for DbGate Web 7.2.1 through an interactive relay shell and SSH. The plugin is under active development; configuration and local installation instructions will be completed with the MVP.

No relay, SSH, or MySQL credentials belong in this repository or in DbGate connection records.

